/**
 * Router entry. Phase 1 uses simple view-state routing (stores/ui.store.ts);
 * a real router is unnecessary until deep-linking or history are needed.
 * This file is the seam where that will be added in a later phase.
 */
export {};
