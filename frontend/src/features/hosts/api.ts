/**
 * Hosts feature API. Phase 1: scaffold only.
 *
 * Phase 2 will export typed wrappers around the generated HostService
 * bindings (list/create/update/delete/test_connection), consumed by
 * useHostsStore and the Host CRUD UI.
 */
export {};
